namespace Domain.Infrastructure.MYS
{
    public static class AuthorizationManager
    {
        //public static bool HasAuth(Guid authId)
        //{
        //    return CurrentUser.Auths.Any(p => p.AuthId == authId);
        //}

        //public static bool HasRole(Guid roleId)
        //{
        //    return CurrentUser.Roles.Any(p => p.RoleId == roleId);
        //}

        //public static bool HasView(Guid viewId)
        //{
        //    return CurrentUser.Views.Any(p => p.ViewId == viewId);
        //}

        //public static bool HasAnyAuth(List<Guid> authIds)
        //{
        //    return CurrentUser.Auths.Any(p => authIds.Contains(p.AuthId));
        //}

        //public static bool HasAnyRole(List<Guid> roleIds)
        //{
        //    return CurrentUser.Roles.Any(p => roleIds.Contains(p.RoleId));
        //}

        //public static bool HasAnyView(List<Guid> viewIds)
        //{
        //    return CurrentUser.Views.Any(p => viewIds.Contains(p.ViewId));
        //}
    }
}
